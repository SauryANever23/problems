#include <stdio.h>
#include <stdlib.h>

int min(int a, int b)
{
  if (a>b)
  {
    return a;
  }
  else 
  {
    return b
  }
}

int main(void)
{
  int m,n,a;
  scanf("%d %d %d", &m, &n, &a);
  
  printf("%d", min(total, a))
}
